import { IdenticonHashDirective } from './identicon-hash.directive';

describe('IdenticonHashDirective', () => {
  it('should create an instance', () => {
    const directive = new IdenticonHashDirective();
    expect(directive).toBeTruthy();
  });
});
